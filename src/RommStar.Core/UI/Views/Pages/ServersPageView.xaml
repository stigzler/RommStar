<Page x:Class="RommStar.Core.UI.Views.Pages.ServersPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:RommStar.Core.UI.Views.Pages"
      xmlns:vms="clr-namespace:RommStar.Core.UI.ViewModels"
      xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
      xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
      xmlns:helpers="clr-namespace:RommStar.Core.UI.Helpers"
      d:DataContext="{d:DesignInstance Type=vms:Pages.ServersPageVM, IsDesignTimeCreatable=True}"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      TextOptions.TextFormattingMode="Display"
      TextOptions.TextRenderingMode="ClearType"
      UseLayoutRounding="True"
      SnapsToDevicePixels="True"
      Title="RomM Servers">

    <Page.Resources>
        <ResourceDictionary>
            <!--<ResourceDictionary.MergedDictionaries>
                <ui:XamlControlsResources />
                <ui:ThemeResources AccentColor="#553e98" />
            </ResourceDictionary.MergedDictionaries>-->

            <BooleanToVisibilityConverter x:Key="BoolToVis" />
        </ResourceDictionary>
    </Page.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Style="{DynamicResource {x:Static ui:ThemeKeys.AccentButtonStyleKey}}"
                    Command="{Binding AddNewServerCommand}" FontWeight="DemiBold"
                     BorderBrush="#80000000" BorderThickness="1" Padding="16,8">
                <StackPanel Orientation="Horizontal">
                    <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Add_20_Regular}" Margin="0,2,5,0" />
                    <TextBlock Text="Add Server" />
                </StackPanel>
            </Button>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding DisplayServers}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,20,10" Background="#40000000" CornerRadius="14" BorderBrush="#80000000" BorderThickness="1">
                            <StackPanel>

                                <!-- Main Card -->
                                <Border  Padding="16,12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <Ellipse Grid.Column="0" Width="14" Height="14" Margin="4,0,16,0" StrokeThickness="1" Stroke="#a0000000"
                                                 Fill="{Binding StatusColor}" VerticalAlignment="Center" />

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">

                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="auto " />
                                                    <RowDefinition Height="auto " />
                                                    <RowDefinition Height="auto " />
                                                    <RowDefinition Height="auto " />
                                                </Grid.RowDefinitions>

                                                <TextBlock Grid.Column ="0" Text="Name:"  Margin="0,5,0,10" ToolTip="Arbitrary name to idenitify this server in RommStar." />
                                                <TextBox Grid.Column ="1" Text="{Binding Server.ServerName, UpdateSourceTrigger=PropertyChanged}"
                                                     VerticalAlignment="Center"
                                                     FontWeight="SemiBold" BorderThickness="1"
                                                     Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="10,0,10,10" />

                                                <TextBlock Grid.Column ="0" Grid.Row="1" Text="Base URL:"  Margin="0,5,0,10" ToolTip="Base URL of the RomM Server. Eg. https://roms.dave.org" />
                                                <TextBox Grid.Column ="1" Grid.Row="1" Text="{Binding Server.BaseUrl, UpdateSourceTrigger=PropertyChanged}"
                                                          VerticalAlignment="Center"
                                                        BorderThickness="1"
                                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="10,0,10,10" />

                                                <TextBlock Grid.Column="0" Grid.Row="2" Text="API Key:" Margin="0,5,0,10" ToolTip="Your API key for this Server" />
                                                <Grid Grid.Column="1" Grid.Row="2">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <PasswordBox Grid.Column ="0"  Loaded="PasswordBox_Loaded"
                                                             ui:PasswordBoxHelper.PasswordRevealMode="{Binding ApiKeyVisibility, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                             helpers:PasswordBoxHelper.Attach="True"
                                                             helpers:PasswordBoxHelper.Password="{Binding Server.ApiToken, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                          VerticalAlignment="Center"
                                                        BorderThickness="1"
                                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="10,0,10,10" />

                                                    <Button Grid.Column="1" Content="👁" ToolTip="Show/Hide Password"
                                                        Width="auto" Command="{Binding ToggleApiKeyVisibilityCommand}" Margin="0,0,10,10" />
                                                </Grid>
                                                <TextBlock Grid.Row="3" Grid.Column="1" Margin="20,0,0,0" Opacity="0.5">
                                                    <Run Text="ID: "/>
                                                    <Run Text="{Binding Server.Id}"/>
                                                </TextBlock>

                                            </Grid>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2" Orientation="Vertical" VerticalAlignment="Top">
                                            <Button Content="Test"
                                                    Command="{Binding DataContext.TestServerConnectionCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    Margin="0,0,0,5" />

                                            <Button Content="Delete"
                                                    Command="{Binding DataContext.DeleteServerCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    Background="#80ff0000" Foreground="white"
                                                    Margin="0,0,0,5" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <!-- Main Card End -->

                                <Border CornerRadius="14" Margin="5"
                                        Background="#a0900000" BorderBrush="#80ff0000" BorderThickness="1" Padding="16,10">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="{Binding HasError, Converter={StaticResource BoolToVis}}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsMessageDismissed}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="✗" Foreground="#F1b0bd" FontWeight="Bold" Margin="0,0,10,0" VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="1" Text="{Binding ErrorMessage}" Foreground="#F1b0bd" FontSize="12" TextWrapping="Wrap" VerticalAlignment="Center" />

                                        <Button Grid.Column="2" Width="24" Height="24" Margin="10,0,0,0" Padding="0"
                                                Background="Transparent" BorderThickness="0" Cursor="Hand"
                                                Command="{Binding DismissMessageCommand}">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="Transparent">
                                                        <TextBlock Text="✕" Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                                                   HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11" FontWeight="SemiBold" />
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </Grid>
                                </Border>

                                <Border CornerRadius="14" Margin="5"
                                        Background="#a0006000" BorderBrush="#8000aa00" BorderThickness="1" Padding="16,10">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="{Binding HasSuccessMessage, Converter={StaticResource BoolToVis}}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsMessageDismissed}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="✓" Foreground="#7BF19E" FontWeight="Bold" Margin="0,0,10,0" VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="1" Text="{Binding SuccessMessage}" Foreground="#7BF19E" FontSize="12" TextWrapping="Wrap" VerticalAlignment="Center" />

                                        <Button Grid.Column="2" Width="24" Height="24" Margin="10,0,0,0" Padding="0"
                                                Background="Transparent" BorderThickness="0" Cursor="Hand"
                                                Command="{Binding DismissMessageCommand}">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="Transparent">
                                                        <TextBlock Text="✕" Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                                                   HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11" FontWeight="SemiBold" />
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
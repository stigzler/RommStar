<ui:FluentWindow x:Class="RommStar.Core.UI.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RommStar.Core.UI.Views"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:viewmodels="clr-namespace:RommStar.Core.UI.ViewModels"
        xmlns:properties="clr-namespace:RommStar.Core.Properties"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowVM, IsDesignTimeCreatable=True}"
        d:Background="#202020" d:WindowStyle="None" d:MinHeight="400" d:MinWidth="600"
        Height="{Binding Source={x:Static properties:Settings.Default},Path=WindowMainSize.Height}"
        Width="{Binding Source={x:Static properties:Settings.Default},Path=WindowMainSize.Width}"
        WindowStartupLocation="CenterScreen"
        Title="RommStar" Closing="FluentWindow_Closing" SizeChanged="FluentWindow_SizeChanged">

    <ui:FluentWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Title="RommStar"
            ShowMaximize="True"
            ShowMinimize="True"
            ShowClose="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/RommStar.Core;component/Resources/rommIcon64px.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <ui:NavigationView Grid.Row="1" x:Name="RootNavigationView"
            MenuItemsSource="{Binding ViewModel.MenuItems, Mode=OneWay}"
            FooterMenuItemsSource="{Binding ViewModel.FooterMenuItems, Mode=OneWay}"
                           IsBackButtonVisible="Collapsed" IsFooterSeparatorVisible="False" IsTopSeparatorVisible="False" OpenPaneLength="150" IsPaneOpen="False" />
    </Grid>
</ui:FluentWindow>